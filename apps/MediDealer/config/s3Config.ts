import { AWS_REGION, AWS_BUCKET_NAME, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY } from '@react-native-dotenv';

export const region = AWS_REGION;
export const bucketName = AWS_BUCKET_NAME;
export const accessKey = AWS_ACCESS_KEY_ID;
export const secretKey = AWS_SECRET_ACCESS_KEY;